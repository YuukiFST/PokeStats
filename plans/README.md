# Implementation Plans

Generated by the improve skill on 2026-08-30. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Focus of this batch: **startup time** (dataset) and **Pokémon image load** (sprites).
Default selection (maintainer: follow the advisor recommendation): plans 001, 002, 003.
Route-level JS code-splitting (`src/App.tsx`) was **not** selected.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Resolve sprites only from a local manifest (no CDN waterfall) | P1 | M | — | DONE |
| 002  | Serve still PNGs in lists; keep ani GIFs for Form detail | P1 | M | 001 | DONE |
| 003  | Unblock the Dex after `core.json`; load sets and learnsets in the background | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **001 then 002.** 002 assumes `spriteUrls(form, kind, base, manifest)` and `{ still?: boolean, ani?: boolean }` on the manifest. Do not start 002 until 001's tests pass.
- **003 is independent** of 001/002. An executor may run 003 in parallel with 001.
- Do not merge 001 and 002 into one commit if that hides a broken manifest-only app (001 must be shippable with ani-only thumbs).

## Approach chosen (so a later audit does not re-litigate)

- **Sprites:** manifest of files that exist + local-only `<img src>`. Rejected: keeping remote CDN fallbacks, HEAD-probing every Form, or a user toggle for online art.
- **Lists vs detail:** still PNG (Showdown gen5) for thumbs; ani GIF for Form/compare/lightbox. Rejected: dropping animation entirely in this batch, or shipping 154 MB of GIFs into every list cell.
- **Dataset:** one `LoadedDataset` + `extrasReady`, not two hooks and not optional `sets` on the type.

## Findings considered and rejected

- Recalculating BST via `enrichment` instead of `calcBST` in the Dex: not worth a plan (cheap arithmetic).
- Precomputing `defensiveProfile` for ~1.3k Forms on load: cheap vs JSON parse; leave it on the core stage.
- Workspace tabs mounting multiple Dex lists: they do not; one `Outlet`.
- Sidebar `logo.png` at 144 px: negligible vs dataset and sprites.
- **PERF-04 / `src/App.tsx` eager routes:** real but smaller than JSON+sprites; not selected. Revisit after 001–003 if first-paint JS is still the limiter.
- **PERF-05 as a standalone plan:** absorbed into 003 (`Promise.all` on extras).

## Verification baseline (repo scripts)

- Tests: `pnpm test` (`vitest run`)
- Lint: `pnpm lint` (`oxlint`)
- Typecheck: `pnpm exec tsc -b --pretty false` (`tsconfig.app.json` has `noEmit: true`)
- There is no `pnpm typecheck` script.
